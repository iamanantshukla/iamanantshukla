# Local Vite env (gitignored — never committed). See .env.example for the documented keys.
# The user's JarvisV2/ Google Drive folder id. The app (signed in as you via OAuth) discovers
# every file inside this folder by name and bootstraps/migrates it on first run.
VITE_JARVIS_FOLDER_ID=1A9Q6l9j3AyVxAPvLPYbYyEcM4tcki_LU
